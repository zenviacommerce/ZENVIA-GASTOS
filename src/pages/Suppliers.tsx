import { Building2 } from 'lucide-react';
import type { Supplier } from '../types';
export function Suppliers({suppliers,onAdd}:{suppliers:Supplier[];onAdd:()=>void}){return <div className="page"><div className="pageHead"><div><div className="eyebrow">MAESTRO</div><h1>Proveedores</h1><p>Proveedores recurrentes y reglas de clasificación.</p></div><button className="primary" onClick={onAdd}>+ Proveedor</button></div>{suppliers.length?<div className="supplierGrid">{suppliers.map(s=><div className="card supplierCard" key={s.id}><div className="supplierBig"><Building2/></div><div><strong>{s.name}</strong><span>{s.supplierType==='goods'?'Mercancía':s.supplierType==='both'?'Mercancía y servicios':'Servicios'}{s.taxId?` · ${s.taxId}`:''}</span>{s.email&&<small>{s.email}</small>}</div></div>)}</div>:<div className="card emptyState large">Los proveedores también se crearán automáticamente al registrar facturas nuevas.</div>}</div>}
